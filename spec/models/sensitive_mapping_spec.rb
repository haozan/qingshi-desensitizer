require 'rails_helper'

RSpec.describe SensitiveMapping, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:sensitive_mapping)
  #   expect(record).to be_persisted
  # end
end
